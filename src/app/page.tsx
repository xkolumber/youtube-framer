import HomePageContent from "@/components/HomePageContent";

const page = () => {
  return (
    <div className="bg-primary/60 h-full">
      <HomePageContent />
    </div>
  );
};

export default page;
